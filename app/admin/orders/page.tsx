import PagePlaceholder from "../components/PagePlaceholder";

export default function AdminOrdersPage() {
  return (
    <PagePlaceholder
      title="Orders"
      description="Track, fulfill, and refund customer orders. Connect this page to your order service to load real data."
    />
  );
}
